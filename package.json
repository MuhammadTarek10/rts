{
  "name": "rts-monorepo-root",
  "private": true,
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.0",
    "@commitlint/cli": "^20.4.2",
    "@commitlint/config-conventional": "^20.4.2"
  },
  "lint-staged": {
    "services/**/*.ts": [
      "eslint --fix"
    ],
    "app/**/*.ts": [
      "eslint --fix"
    ],
    "app/**/*.vue": [
      "eslint --fix"
    ]
  }
}
